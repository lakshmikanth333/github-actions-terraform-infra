name: aws resource creation
on: 
  push:
    branches:
    - main

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
    - name: check-out repo code
      uses: actions/checkout@v4

    - name: list
      run: ls -l